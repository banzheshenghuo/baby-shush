# 婴儿嘘嘘哄睡

给新手爸妈的「嘘嘘（Shush）」哄睡小工具：打开即播放柔和的「嘘」声白噪音，中间是缓慢的呼吸律动圆圈，轻触圆圈即可播放 / 暂停。支持锁屏、息屏后台持续播放，可安装到手机主屏幕当 App 用。

## 声音方案

主音源为随仓库分发的真实「嘘」声录音 `audio/xuxu.mp3`（约 2 分 47 秒，循环播放），由 `<audio>` 元件播放——因此可以享受系统媒体通道：**锁屏 / 息屏 / 后台继续播放**，锁屏界面显示封面与播放控件。录音已纳入 Service Worker 预缓存，离线同样可用。

若录音文件异常加载失败，页面自动回退到程序化合成的「嘘」声（固定种子：棕噪声 + 低频起伏 + 首尾 1 秒等功率交叉淡化的 12 秒无缝循环），保证任何情况下都有一段柔和音源可播。

## 使用

- 浏览器打开 <https://banzheshenghuo.github.io/baby-shush/>，进入页面即自动开始播放
- **轻触中间的呼吸圆圈**：播放 ⇄ 暂停（呼吸律动随播放状态同步启停）
- 音量完全由设备音量键控制；锁屏 / 耳机线控 / 车载控件同样可控
- 「添加到主屏幕」后即为全屏 PWA，支持离线打开

## 平台说明（如实）

- **Android（Chrome/Edge 等）**：锁屏、息屏持续播放，锁屏显示控件 ✓
- **iOS（Safari / 主屏 PWA）**：播放中的 HTMLAudioElement 在锁屏后通常可持续；但 iOS 不允许网页自动出声，**每次打开需轻触圆圈一次**才能开始。这是平台限制，所有网页应用相同
- 桌面浏览器：一切功能可用；被浏览器自动播放策略拦截时，轻触圆圈即可

## 功能清单

- 打开即播（自动播放被拦截时一键轻触兜底）
- 呼吸律动圆圈，点按切换播放 / 暂停
- 锁屏 / 息屏 / 后台播放（Media Session API）
- 音量由设备硬件控制，页面零音量逻辑
- PWA：可安装、离线可用（Service Worker 网络优先 + 缓存回退）
- 尊重系统「减弱动态效果」设置
- 零依赖、零构建，音源本地自带（真实录音 + 合成兜底，无任何外链）

## 开发与测试

仿真测试在 Node 虚拟机里加载真实 `app.js`，用模拟的 Audio / MediaSession / localStorage 断言全部交互逻辑、录音音源、加载失败回退与合成 WAV 格式：

```bash
node --check app.js       # 语法检查
node test/simulation.mjs  # 仿真测试
```

推送 main 时 GitHub Actions 自动执行（`.github/workflows/test.yml`）。

图标 PNG 由零依赖脚本从 `icon.svg` 同参数渲染（新月 + 夜空配色）。

## 在线使用

GitHub Pages：<https://banzheshenghuo.github.io/baby-shush/>

## License

MIT（见 [LICENSE](LICENSE)）
